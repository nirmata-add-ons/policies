resources:
  - disallow_latest_tag/disallow_latest_tag.yaml 
  
  
patches:
  - patch: |-
      - op: replace
        path: /spec/validationFailureAction
        value: Audit
    target:
      kind: ClusterPolicy 
  
